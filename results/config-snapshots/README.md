# Historical flight-controller parameters

`vio_ardupilot.params` is a retained historical export, not the current aircraft
configuration or a recommended parameter set. Its capture date has not been
independently verified during the repository reorganization.

Keep future exports with the corresponding experiment, recording their capture
date and firmware revision. Mission plans are separate, under `src/missions/`
at the repository root.
